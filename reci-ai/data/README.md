# Data Directory

This directory holds bundled dataset files used for the demo/auto-init.

Files here are populated at build time by `build_setup.py`:
- `sample_candidates.json` — copied from `Dataset/` folder
- `job_description.docx` — copied from `Dataset/` folder

These files are NOT committed to git (they are generated during build).
